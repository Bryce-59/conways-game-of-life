INT(1, 100, t) NEWLINE
NEWLINE
REPI(testno, t, NEWLINE)
    ASSERT(testno < t)
    
    INT(1, 200, r) SPACE
    INT(1, 200, c) NEWLINE

    SET(n = 0)
    SET (area = r * c)
    IF (area > 700)
        INT(1, 700,  m) NEWLINE
        SET(n = m)
    ELSE
        INT(1, area, m) NEWLINE
        SET(n = m)
    END
    
    REPI(coor, n, NEWLINE)
        INT(1, r, y) SPACE
        INT(1, c, x) NEWLINE
    END

    INT(1, 2000, s) SPACE
    INT(1, 200, f) NEWLINE
END
EOF