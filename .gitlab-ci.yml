before_script:
    - date
    - uname -a
    - printenv | sort

gcc:
    image: gpdowning/gcc
    script:
    - cat Makefile
    - make versions
    - ls -al
    - make test
    - ls -al
    - make ../cs371p-allocator-tests
    - ls -al ../cs371p-allocator-tests/
    - ls -al
    - make run-conway
    - ls -al
    - make run-fredkin
    - ls -al
    - make run-cell
    - ls -al
    # - make run-all
    # - ls -al
    - make check
    - ls -al
